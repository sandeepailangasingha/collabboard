{
  "name": "syncboard-fullstack",
  "version": "1.0.0",
  "description": "Full-stack collaborative Kanban board with Real-Time WebSockets and MongoDB Atlas",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "node server/src/server.js",
    "server": "npm --prefix server start",
    "client": "npm --prefix client dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
